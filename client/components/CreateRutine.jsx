import { StyleSheet, Text, View, ImageBackground, TextInput, Pressable } from 'react-native';

export default function CreateRutine({ setFirstLogin }) {
    return (
        <View>
            <Text>Crear rutina</Text>
        </View>
    )
}

const styles = StyleSheet.create({

})